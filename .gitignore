# 各論文は papers/ 配下の独立した（非公開）リポジトリとして管理する。
# 公開ワークスペースには論文の中身を含めない。
/papers/*
!/papers/.gitkeep

# 自動ビルドの状態（paper ごとのハッシュ・ロック）
/.cache/

# Claude Code のローカル設定（workspace では settings.json のみ追跡する）
/.claude/settings.local.json

# OS / エディタ
.DS_Store
